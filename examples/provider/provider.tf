terraform {
  required_providers {
    epilot-saved-view = {
      source  = "epilot-dev/epilot-saved-view"
      version = "0.10.3"
    }
  }
}

provider "epilot-saved-view" {
  # Configuration options
}